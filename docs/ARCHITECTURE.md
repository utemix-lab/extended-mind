# Архитектура системы

> **Единый онтологический граф — единственный источник истины**

---

## Обзор

Система построена на принципе **единого канонического графа** (Universe Graph), который является источником истины для всех компонентов.

```
┌─────────────────────────────────────────────────────────────────────┐
│                    extended-mind                                    │
│                                                                     │
│   • Редактор канона (editor.html)                                  │
│   • Хранитель онтологии                                            │
│   • Единственный writer канона                                     │
│   • В перспективе: RAG/LLM для интроспекции                        │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ universe.json (SSoT)
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      contracts                                      │
│                                                                     │
│   • UI контракты (layout, interaction, bindings)                   │
│   • Ассеты и тексты                                                │
│   • Состояния сессий                                               │
│   • НЕ определяет структуру — только интерпретирует канон          │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      dream-graph (витрина)                          │
│                                                                     │
│   • 3D визуализация канона                                         │
│   • Чистый рендер — read-only                                      │
│   • Views (фильтры), но не меняет граф                             │
│   • Деплоится на GitHub Pages                                       │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      godot-sandbox (опциональный)                   │
│                                                                     │
│   • Экспериментальный рендер                                       │
│   • "Строительные леса"                                            │
│   • Read-only consumer контрактов                                  │
│   • Может быть удалён без изменения канона                         │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Ключевые принципы

### 1. Единый источник истины (SSoT)

**universe.json** — единственный файл, определяющий структуру системы:
- Узлы (nodes) — онтологические сущности
- Связи (edges) — отношения
- Метаданные — Node Contract v1
- Контент — Story/System/Service

Никакой другой файл не определяет структуру.

### 2. Рендеры не пишут структуру

```
extended-mind ──► universe.json ◄── dream-graph (read-only)
                                ◄── godot-sandbox (read-only)
```

Только extended-mind может редактировать канон.

### 3. Views — не графы

Граф один. Views — это проекции:

| View | Фильтр |
|------|--------|
| Knowledge | domain, concept, character |
| System | root, hub, module, spec, process, policy |
| All | все узлы |

### 4. Структура vs Состояние

| Структура (канон) | Состояние (не канон) |
|-------------------|----------------------|
| nodes, edges | camera, selection |
| type, role, semantics | layout positions |
| content | UI session data |

Состояние хранится в `contracts/sessions/`.

---

## Node Contract v1

Каждый узел — онтологическая сущность, а не "контент".

### Обязательные поля

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | string | Глобально уникальный, вечный |
| `label` | string | Отображаемое имя |

### Поля с дефолтами

| Поле | Значения | Дефолт |
|------|----------|--------|
| `type` | root, hub, domain, concept, character, module, spec, process, policy, artifact | concept |
| `visibility` | public, internal, hidden | public |
| `status` | core, expandable, frozen, deprecated | expandable |
| `semantics.role` | description, container, mediator, content | content |
| `semantics.abstraction` | high, medium, low | medium |
| `rag.index` | boolean | true |
| `rag.priority` | 0–1 | 0.5 |

### Контент

| Поле | Описание |
|------|----------|
| `story` | Нарративный контент |
| `system` | Техническое описание |
| `service` | Доступные действия |

---

## Типы связей

| Тип | Назначение |
|-----|------------|
| `structural` | Скелет системы |
| `contains` | Контейнер → элемент |
| `relates` | Смысловая связь |
| `describes` | Описывает |
| `implements` | Реализует |
| `renders` | Визуализирует |

---

## System Graph

В каноне существует ветка, описывающая саму систему:

```
system (root)
└── system-graph (hub)
    ├── module-extended-mind (module)
    ├── module-dream-graph (module)
    ├── module-contracts (module)
    ├── policy-ssot (policy)
    └── process-edit-to-render (process)
```

Эти узлы видны в System view. Так система "знает себя".

---

## Репозитории

| Репозиторий | Роль | Права на канон |
|-------------|------|----------------|
| **extended-mind** | Ядро, редактор | Writer |
| **contracts** | Данные, ассеты | Хранилище |
| **dream-graph** | Витрина | Read-only |
| **godot-sandbox** | Режиссура | Read-only |

---

## Самоосознание без субъектности

Система "осознаёт себя" не через тексты, а через:

1. **Канон** — формальное описание структуры
2. **Инварианты** — правила, которые нельзя нарушать
3. **Валидацию** — способность проверить себя
4. **Retrieval (RAG)** — способность ответить на вопросы о себе, читая канон

Никакой "личности". Только структура + проверка + извлечение.

---

## Текущий статус

| Компонент | Статус |
|-----------|--------|
| Canon loader | ✅ Реализован |
| Node Contract v1 | ✅ Внедрён |
| Views | ✅ Работают |
| System Graph | ✅ Добавлен |
| Validator | 📋 Планируется |
| Content refs | 📋 Планируется |
| RAG | 📋 В перспективе |

---

## Что нельзя делать

❌ Заводить второй "граф графа"  
❌ Редактировать канон из рендеров  
❌ Смешивать документацию с каноном без узлов System Graph  
❌ Хранить истину в README вместо universe.json

---

## Короткая формула

> Это система с единым онтологическим графом, где и знания, и сама система являются узлами одного канона, а все интерфейсы — лишь проекции этого канона.
