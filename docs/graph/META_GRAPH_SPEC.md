# Universe Graph — Спецификация мета-графа

> Типы узлов и связей для мета-уровня экосистемы utemix-lab.

## Слои (3S)

Каждый узел принадлежит одному из трёх слоёв:

| Слой | Код | Описание | Цвет |
|------|-----|----------|------|
| **Story** | `story` | Что происходит (контент, истории) | 🟡 Жёлтый |
| **System** | `system` | Как устроено (архитектура, решения) | 🔵 Синий |
| **Service** | `service` | Что делать (действия, экспорты) | 🟢 Зелёный |

---

## Типы узлов

### Universe (экосистема)

| Тип | Слой | Описание | RAG по умолчанию |
|-----|------|----------|------------------|
| `Project` | system | Проект экосистемы | description |
| `Tool` | service | Инструмент (среда со вкладками) | description |
| `Repository` | system | Git-репозиторий | — |

### Architecture (архитектура)

| Тип | Слой | Описание | RAG по умолчанию |
|-----|------|----------|------------------|
| `Decision` | system | Архитектурное решение (ADR) | ✅ всё |
| `Pattern` | system | Паттерн / методика | ✅ всё |
| `Principle` | system | Принцип | ✅ всё |
| `Contract` | system | Контракт данных | description |

### Ontology (онтология)

| Тип | Слой | Описание | RAG по умолчанию |
|-----|------|----------|------------------|
| `Concept` | system | Понятие / термин | ✅ всё |
| `Definition` | system | Определение | ✅ всё |
| `Layer` | system | Слой системы (3S) | — |

### Actors (роли)

| Тип | Слой | Описание | RAG по умолчанию |
|-----|------|----------|------------------|
| `Role` | system | Роль в системе | description |
| `Agent` | service | Конкретный агент | — |

### Artifacts (артефакты)

| Тип | Слой | Описание | RAG по умолчанию |
|-----|------|----------|------------------|
| `Document` | story | Документ | ✅ всё |
| `Scene` | story | Сцена / вкладка | description |
| `Export` | service | Экспорт данных | — |
| `Episode` | story | Эпизод истории | ✅ всё |

---

## Типы связей

### SYMBOLIC (канонические)

| Тип | Семантика | Пример |
|-----|-----------|--------|
| `governs` | A управляет B | `extended-mind` → `vovaipetrova-core` |
| `implements` | A реализует B | `dream-graph` → `Cosmos Map` |
| `defines` | A определяет B | `VISION.md` → `Principle:Backend=ecosystem` |
| `contains` | A содержит B | `Project:extended-mind` → `Tool:console` |
| `uses` | A использует B | `Tool:dream-graph` → `Export:cosmos-map` |
| `derives_from` | A производная от B | `minds-lorgnette` → `extended-mind` |
| `feeds` | A питает B | `evoquant` → `vovaipetrova-core` |

### Roles (роли)

| Тип | Семантика | Пример |
|-----|-----------|--------|
| `owns` | A владеет B | `Role:Author` → `Decision:*` |
| `proposes` | A предлагает B | `Agent:Codex` → `GraphDelta:*` |
| `accepts` | A принимает B | `Role:Author` → `GraphDelta:*` |
| `rejects` | A отклоняет B | `Role:Author` → `GraphDelta:*` |

### 3S (ракурсы)

| Тип | Семантика | Пример |
|-----|-----------|--------|
| `narrates` | A рассказывает о B | `Layer:Story` → `Document:*` |
| `exposes` | A показывает устройство B | `Layer:System` → `Pattern:*` |
| `enables` | A позволяет действие над B | `Layer:Service` → `Export:*` |

### VECTOR (гипотетические)

| Тип | Семантика | RAG |
|-----|-----------|-----|
| `semantic_near` | A семантически близок к B | Да |
| `similar_to` | A похож на B | Да |

---

## Свойства узла

### Core (обязательные)

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | string | Уникальный идентификатор |
| `label` | string | Человекочитаемое название |
| `type` | enum | Тип узла (из списка выше) |
| `layer` | enum | Слой: `story` / `system` / `service` |
| `status` | enum | `draft` / `active` / `stable` / `archived` |

### Ecosystem (экосистемные)

| Поле | Тип | Описание |
|------|-----|----------|
| `project_id` | string | ID проекта-владельца |
| `feeds` | string[] | Какие проекты питает |
| `derives_from` | string | От чего производный |

### RAG (векторизация)

| Поле | Тип | Описание |
|------|-----|----------|
| `rag_include` | boolean | Включить в RAG |
| `rag_fields` | string[] | Какие поля векторизовать |

### Custom (пользовательские)

Любые дополнительные поля в объекте `props`.

---

## RAG по умолчанию

При создании узла определённых типов автоматически включается RAG:

| Тип | `rag_include` | `rag_fields` |
|-----|---------------|--------------|
| `Concept` | ✅ true | `["label", "description", "props"]` |
| `Definition` | ✅ true | `["label", "description", "props"]` |
| `Decision` | ✅ true | `["label", "description", "context", "decision"]` |
| `Principle` | ✅ true | `["label", "description"]` |
| `Pattern` | ✅ true | `["label", "description"]` |
| `Document` | ✅ true | `["label", "content"]` |
| `Episode` | ✅ true | `["label", "description"]` |
| `Project` | ⚠️ partial | `["description"]` |
| `Tool` | ⚠️ partial | `["description"]` |
| Остальные | ❌ false | `[]` |

---

## Правила консистентности

### Структурные (автоматические)

| Правило | Описание |
|---------|----------|
| `orphan_check` | Узел без связей → предупреждение |
| `type_layer_match` | Тип узла соответствует слою |
| `required_edges` | `Tool` должен иметь `derives_from` или `contains` |
| `duplicate_check` | Нет узлов с одинаковым `id` |
| `layer_balance` | Все три слоя (3S) представлены |

### Семантические (LLM)

| Правило | Описание |
|---------|----------|
| `edge_rationale` | Связь имеет обоснование |
| `concept_clarity` | Концепт имеет чёткое определение |
| `decision_context` | Решение имеет контекст и альтернативы |

---

## Формат экспорта

```json
{
  "schema": "universe-graph.v0.1",
  "meta": {
    "project": "extended-mind",
    "version": "0.1.0",
    "exported_at": "2026-01-17T12:00:00Z"
  },
  "nodes": [
    {
      "id": "project:extended-mind",
      "label": "extended-mind",
      "type": "Project",
      "layer": "system",
      "status": "active",
      "rag_include": true,
      "rag_fields": ["description"],
      "props": {
        "description": "Мета-координатор экосистемы"
      }
    }
  ],
  "edges": [
    {
      "id": "edge:1",
      "source": "project:extended-mind",
      "target": "project:vovaipetrova-core",
      "type": "governs",
      "layer": "symbolic"
    }
  ]
}
```
