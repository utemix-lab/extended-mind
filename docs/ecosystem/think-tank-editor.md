# Think Tank — Core Taxonomy Editor

> Визуализатор и редактор таксономии vovaipetrova-core в формате графа.  
> Часть экосистемного слоя extended-mind.

## Назначение

**Think Tank** — каноническое представление архитектуры проекта vovaipetrova-core.  
Экосистема подключается как аккуратное наложение (overlay), не ломая текущий процесс.

## Расположение

```
extended-mind/
└── spaces/
    └── extended-mind-console/
        └── core-taxonomy/
            ├── index.html    # Основной редактор
            └── README.md     # Документация
```

## Скриншот

![Think Tank Editor](https://github.com/utemix-lab/extended-mind/raw/main/docs/ecosystem/assets/think-tank-screenshot.png)

## Три слоя рёбер

| Слой | Визуал | Типы связей | Описание |
|------|--------|-------------|----------|
| **SYMBOLIC** | Сплошная синяя | `links_to`, `mentions`, `has_tag`, `alias_of`, `related_to`, `child_of`, `governs`, `implements` | Жёсткие связи: slug, теги, фасеты, ручные |
| **VECTOR** | Пунктир фиолетовый | `semantic_near`, `similar_to` | Мягкие связи с ползунком порога `min_score` |
| **UNIVERSE** | Точечная розовая | `ecosystem_related`, `cross_project`, `pattern_applies` | Межпроектные связи (read-only overlay) |

## Режимы отображения

### 🎯 Project Focus (по умолчанию)

Показывает только подграф V&P (Think Tank) как базовый вид.  
Экосистемные узлы и рёбра скрыты.

**Когда использовать:**
- Работа с таксономией одного проекта
- Проверка структуры страниц/терминов/сторис
- Редактирование связей внутри проекта

### 🌐 Ecosystem Overlay

Показывает узлы/рёбра, которые соприкасаются с текущим подграфом.  
Позволяет видеть коммуникации проекта с "внешним миром".

**Когда использовать:**
- Анализ межпроектных зависимостей
- Проверка экосистемных гипотез
- Планирование интеграций

## Focus + Context

При клике на узел:
- Узел становится **focused** (белая рамка)
- Показываются соседи в N-хопах (по умолчанию 1)
- Остальные узлы затемняются

### Контролы

| Элемент | Действие |
|---------|----------|
| **−/+** | Изменить глубину N-хопов (1–5) |
| **Сбросить фокус** | Показать весь граф |
| По умолчанию | 1-хоп для производительности |

### Производительность

- 1-хоп окрестность по умолчанию
- Ленивая загрузка соседей
- Сэмплинг VECTOR рёбер при большом k
- Ограничение видимых меток

## Фильтры

| Фильтр | Описание |
|--------|----------|
| **Поиск** | По slug, title, tag |
| **Типы узлов** | page, term, story, tag, project, pattern |
| **Статусы** | active, draft, archived, hypothesis |
| **Слои рёбер** | SYMBOLIC, VECTOR, UNIVERSE |
| **min_score** | Порог для VECTOR рёбер (0.00–1.00) |

## Provenance

При клике на узел показывается его происхождение:

| Поле | Описание |
|------|----------|
| `stable_id` | Стабильный идентификатор |
| `project_id` | Проект-владелец |
| `source` | Источник данных (файл экспорта) |
| `version` | Версия записи |
| `updated_at` | Время обновления |

## Редактирование

### View Mode
- Только просмотр
- Узлы нельзя перетаскивать
- Фильтры и Focus работают

### Edit Mode
- Перетаскивание узлов
- Редактирование свойств узлов и рёбер
- Добавление/удаление узлов и рёбер

## Delta Inbox (📬 Предложить связь)

Позволяет предложить новую связь без прямого изменения данных:

1. Нажмите **📬 Предложить связь**
2. Кликните узел-источник
3. Кликните узел-цель
4. Дельта сохраняется в localStorage

### Формат дельты

```json
{
  "delta_type": "candidate_edge",
  "from": "term:aliases",
  "to": "term:autolink",
  "edge_type": "related_to",
  "rationale": "",
  "confidence": "low",
  "source": "extended-mind-editor",
  "timestamp": "2026-01-16T..."
}
```

### Workflow

```
Редактор → localStorage → Экспорт JSON → PR в vovaipetrova-core/data/graph/inbox/
```

## Сохранение

| Действие | Описание |
|----------|----------|
| **💾 Сохранить** | Сохранить сессию в localStorage |
| **📤 Экспорт** | Скачать JSON-файл сессии (с pending deltas) |
| **🔄 GitHub** | Перезагрузить данные из vovaipetrova-core |
| **📁 Файл** | Загрузить JSON-файл |

## Источники данных

| Источник | URL | Содержимое |
|----------|-----|------------|
| `canon_map.v1.json` | GitHub Raw | Страницы (page) |
| `kb_terms.v1.jsonl` | GitHub Raw | Термины KB (term) |
| `stories.v1.jsonl` | GitHub Raw | Эпизоды Stories (story) |
| `graph.jsonl` | GitHub Raw | VECTOR рёбра и экосистемные узлы |

## Принцип изоляции

```
vovaipetrova-core    →    READ-ONLY source
                    ↓
             Think Tank Editor
                    ↓
extended-mind        →    Saved sessions + proposed deltas
```

| Операция | Источник/цель |
|----------|---------------|
| **Чтение** | vovaipetrova-core (GitHub raw) |
| **Запись** | extended-mind (localStorage / экспорт) |
| **Deltas** | Предложения → inbox vovaipetrova-core |

## Типы узлов

| Тип | Цвет | Источник |
|-----|------|----------|
| `page` | 🟡 Жёлтый | canon_map.v1.json |
| `term` | 🟣 Фиолетовый | kb_terms.v1.jsonl |
| `story` | 🔵 Голубой | stories.v1.jsonl |
| `tag` | 🟢 Зелёный | Извлечено из tags |
| `project` | 🩷 Розовый | Экосистемные узлы |
| `pattern` | 🟠 Оранжевый | Экосистемные узлы |

## Типы рёбер

### SYMBOLIC (жёсткие)

| Тип | Описание |
|-----|----------|
| `links_to` | Явная ссылка в тексте |
| `mentions` | Упоминание термина |
| `has_tag` | Страница → тег |
| `alias_of` | Синоним |
| `related_to` | Связанные темы |
| `child_of` | Иерархия |
| `governs` | Управляет |
| `implements` | Реализует |

### VECTOR (мягкие)

| Тип | Описание |
|-----|----------|
| `semantic_near` | Семантическая близость |
| `similar_to` | Похожие по контексту |

### UNIVERSE (межпроектные)

| Тип | Описание |
|-----|----------|
| `ecosystem_related` | Связь в экосистеме |
| `cross_project` | Межпроектная зависимость |
| `pattern_applies` | Паттерн применим |

## Дорожная карта

| Фаза | Описание | Статус |
|------|----------|--------|
| **1** | Think Tank как основной граф-вид | ✅ |
| **2** | Universe overlay (read-only экосистема) | ✅ |
| **3** | Интеракции: подсветка конфликтов/дельт | ⏳ |
| **4** | Step-by-step review workflow | планируется |
| **5** | RAG-graph интеграция | планируется |

## Риски и митигация

| Риск | Решение |
|------|---------|
| Когнитивная перегрузка | Режимы (Symbolic/Vector/Overlay) раздельно |
| Производительность | 1-хоп по умолчанию, остальное по запросу |
| Консистентность | Think Tank = source of truth; граф-рендер = витрина |
| Неконтролируемые изменения | Read-only источник + Delta inbox |

## Связанные документы

- [Экосистемный слой](./index.md) — общая архитектура
- [Cosmos Map](../cosmos/index.md) — формат графа
- [ECOSYSTEM_LAYER.md](../../ecosystem/ECOSYSTEM_LAYER.md) — правила
- [UNIVERSE_SPEC.md](https://github.com/utemix-lab/vovaipetrova-core/blob/main/docs/graph/UNIVERSE_SPEC.md) — спецификация графа
