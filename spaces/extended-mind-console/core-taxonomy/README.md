# Think Tank — Core Taxonomy Editor

Визуализатор и редактор таксономии `vovaipetrova-core` в формате графа.

## Концепция

**Think Tank** — это каноническое представление архитектуры проекта vovaipetrova-core.  
Экосистема подключается как аккуратное наложение (overlay), не ломая текущий процесс.

## Три слоя рёбер

| Слой | Визуал | Типы связей | Описание |
|------|--------|-------------|----------|
| **SYMBOLIC** | Сплошная линия | `links_to`, `mentions`, `has_tag`, `alias_of`, `related_to`, `child_of`, `governs`, `implements` | Жёсткие связи: slug, теги, фасеты, ручные |
| **VECTOR** | Пунктир | `semantic_near`, `similar_to` | Мягкие связи с ползунком порога `min_score` |
| **UNIVERSE** | Точечная линия | `ecosystem_related`, `cross_project`, `pattern_applies` | Межпроектные связи (read-only overlay) |

## Режимы отображения

### 🎯 Project Focus (по умолчанию)
Показывает только подграф V&P (Think Tank) как базовый вид.  
Экосистемные узлы и рёбра скрыты.

### 🌐 Ecosystem Overlay
Показывает узлы/рёбра, которые соприкасаются с текущим подграфом.  
Позволяет видеть коммуникации проекта с "внешним миром".

## Focus + Context

При клике на узел:
- Узел становится **focused** (белая рамка)
- Показываются соседи в N-хопах (по умолчанию 1)
- Остальные узлы затемняются

Контролы:
- **−/+** — изменить глубину N-хопов (1–5)
- **Сбросить фокус** — показать весь граф

## Фильтры

| Фильтр | Описание |
|--------|----------|
| **Поиск** | По slug, title, tag |
| **Типы узлов** | page, term, story, tag, project, pattern |
| **Статусы** | active, draft, archived, hypothesis |
| **Слои рёбер** | SYMBOLIC, VECTOR, UNIVERSE |
| **min_score** | Порог для VECTOR рёбер (0.00–1.00) |

## Provenance

При клике на узел показывается его происхождение:
- `stable_id` — стабильный идентификатор
- `project_id` — проект-владелец
- `source` — источник данных (файл экспорта)
- `version` — версия
- `updated_at` — время обновления

## Редактирование

### View Mode
Только просмотр. Узлы нельзя перетаскивать.

### Edit Mode
- Перетаскивание узлов
- Редактирование свойств узлов и рёбер
- Добавление/удаление узлов и рёбер

## Кнопка "Предложить связь" (📬 Delta Inbox)

Позволяет предложить новую связь без прямого изменения данных:
1. Нажмите 📬 **Предложить связь**
2. Кликните узел-источник
3. Кликните узел-цель
4. Дельта сохраняется в localStorage

Дельты экспортируются вместе с сессией и могут быть поданы в `vovaipetrova-core/data/graph/inbox/`.

## Сохранение

| Действие | Описание |
|----------|----------|
| **💾 Сохранить** | Сохранить сессию в localStorage |
| **📤 Экспорт** | Скачать JSON-файл сессии |
| **🔄 GitHub** | Перезагрузить данные из vovaipetrova-core |
| **📁 Файл** | Загрузить JSON-файл |

## Источники данных

| Источник | URL |
|----------|-----|
| `canon_map.v1.json` | Страницы (page) |
| `kb_terms.v1.jsonl` | Термины KB (term) |
| `stories.v1.jsonl` | Эпизоды Stories (story) |
| `graph.jsonl` | VECTOR рёбра и экосистемные узлы |

## Принципы изоляции

```
vovaipetrova-core    →    READ-ONLY source
                    ↓
             Think Tank Editor
                    ↓
extended-mind        →    Saved sessions + proposed deltas
```

- **Чтение**: из vovaipetrova-core (GitHub raw)
- **Запись**: только в extended-mind (localStorage / экспорт)
- **Deltas**: предложения связей → inbox vovaipetrova-core

## Дорожка вперёд

| Фаза | Описание |
|------|----------|
| **1** | Think Tank как основной граф-вид (Symbolic + Vector) ✅ |
| **2** | Universe overlay (read-only экосистема) ✅ |
| **3** | Интеракции: подсветка конфликтов/дельт, step-by-step review |

## Риски и как не перегрузиться

| Риск | Решение |
|------|---------|
| Когнитивная перегрузка | Режимы (Symbolic/Vector/Overlay) раздельно |
| Производительность | 1-хоп по умолчанию, остальное по запросу |
| Консистентность | Think Tank = source of truth; граф-рендер = витрина |
