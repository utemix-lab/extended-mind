# extended-mind

**Ядро системы** — хранитель канонического графа и источник истины.

> 📖 **[SINGLE-SOURCE-PLAYBOOK.md](./docs/SINGLE-SOURCE-PLAYBOOK.md)** — полный замысел системы для быстрого старта с любым агентом.

---

## Цель

Построить самоописывающуюся систему, основанную на едином каноническом графе:

- **Единый источник истины (SSoT)** — `universe.json`
- **Визуализация разными рендерами** — dream-graph, godot-sandbox
- **Интроспекция** — система может прочитать и понять себя (через RAG, в перспективе)

> Система "понимает себя" в смысле: знает, из каких частей состоит, какие у них роли, какие связи допустимы, и умеет проверить собственную целостность.

---

## Архитектура

```
┌─────────────────────────────────────────────────────────────────┐
│                    extended-mind (здесь)                        │
│                                                                 │
│   • Редактор канона (editor.html)                              │
│   • Хранитель онтологии                                        │
│   • Место эволюции системы                                     │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ universe.json
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      contracts                                  │
│                                                                 │
│   • UI контракты (layout, interaction, bindings)               │
│   • Ассеты и тексты                                            │
│   • Состояния сессий                                           │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      dream-graph (витрина)                      │
│                                                                 │
│   • 3D визуализация канона                                     │
│   • Чистый рендер — не источник истины                         │
│   • Публикуется на GitHub Pages                                │
└─────────────────────────────────────────────────────────────────┘
```

---

## Канон: Universe Graph

### Что является истиной

Единственный источник истины — `universe.json`.

В нём:
- **Узлы (nodes)** — онтологические сущности
- **Связи (edges)** — отношения между сущностями
- **Метаданные узлов** — Node Contract v1
- **Контент** — Story/System/Service

Никакой другой файл не определяет структуру системы.

### Node Contract v1

```json
{
  "id": "уникальный-id",
  "label": "Отображаемое имя",
  "type": "root | hub | domain | concept | character | module | spec | process | policy | artifact",
  "visibility": "public | internal | hidden",
  "status": "core | expandable | frozen | deprecated",
  "semantics": {
    "role": "description | container | mediator | content",
    "abstraction": "high | medium | low"
  },
  "rag": {
    "index": true,
    "priority": 0.5
  },
  "story": "Нарративный контент",
  "system": "Техническое описание",
  "service": "Доступные действия"
}
```

### Типы связей

| Тип | Назначение |
|-----|------------|
| `structural` | Скелет системы |
| `contains` | Контейнер → элемент |
| `relates` | Смысловая связь |
| `describes` | Описывает |
| `implements` | Реализует |
| `renders` | Визуализирует |

> Нет второго графа. Есть один граф и разные способы его читать (Views).

---

## Views (проекции)

Граф один. Разные — режимы просмотра:

| View | Показывает |
|------|------------|
| **Knowledge** | domain, concept, character |
| **System** | root, hub, module, spec, process, policy |
| **All** | все узлы |

Views = фильтры + визуальные правила. Никаких копий графа.

---

## System Graph (самоописание)

В каноне существует ветка System Graph, которая описывает саму систему:

- `system` (root)
- `system-graph` (hub)
- `module-extended-mind`
- `module-dream-graph`
- `module-contracts`
- `policy-ssot`
- `process-edit-to-render`

Эти узлы видны в System view. Так система "знает себя".

---

## Быстрый старт

### Редактор графа

Открыть в браузере:
```
contracts/public/graph/editor.html
```

### Редактирование

1. **+ Node** — добавить узел
2. **+ Edge** — соединить узлы
3. **Клик на узел** — редактировать Story/System/Service
4. **Save JSON** — сохранить граф

### Результат

Файл `universe.json` кладётся в:
```
contracts/public/graph/universe.json
```

Далее нужно синхронизировать снапшот для рендера:
```
dream-graph\scripts\sync-contracts.ps1
```

И закоммитить изменения в `dream-graph` (для Pages).

---

## Структура репозитория

```
extended-mind/
├── docs/
│   ├── ARCHITECTURE.md         # Архитектура системы
│   ├── UNIVERSE_GRAPH_SPEC.md  # Спецификация universe.json
│   ├── ROADMAP.md              # План развития
│   ├── index.md                # Оглавление
│   └── _archive/               # Архив прежних попыток
├── README.md                   # Этот файл
└── LICENSE
```

---

## Принципы

### 1. Единый источник истины

`universe.json` — единственное место определения структуры системы.

### 2. Рендеры не пишут структуру

dream-graph и godot-sandbox только читают канон.

### 3. Структура vs Состояние

| Структура (канон) | Состояние (не канон) |
|-------------------|----------------------|
| nodes, edges | camera, selection |
| type, role, semantics | layout positions |
| content refs | UI session data |

Состояние хранится в `contracts/sessions/`.

### 4. Самоосознание без субъектности

Система "осознаёт себя" не через тексты, а через:
- **Канон** — формальное описание структуры
- **Инварианты** — правила, которые нельзя нарушать
- **Валидацию** — способность проверить себя
- **Retrieval (RAG)** — способность ответить на вопросы о себе

Никакой "личности". Только структура + проверка + извлечение.

---

## Перспективы

- [ ] Validator — CLI self-check канона
- [ ] Content refs — убрать большие тексты из JSON
- [ ] RAG — интроспекция через retrieval
- [ ] Author View — вспомогательный слой для автора

---

## Связанные репозитории

| Репозиторий | Роль | Статус |
|-------------|------|--------|
| **contracts** | Данные, ассеты | Активный |
| **dream-graph** | Витрина — финальный рендер | Активный |
| **godot-sandbox** | Режиссура UI | Строительные леса |

---

## Главное, что нельзя делать

❌ Заводить второй "граф графа"  
❌ Редактировать канон из рендеров  
❌ Смешивать документацию с каноном без узлов System Graph  
❌ Хранить истину в README вместо universe.json

---

## Короткая формула

> Это система с единым онтологическим графом, где и знания, и сама система являются узлами одного канона, а все интерфейсы — лишь проекции этого канона.

---

## Лицензия

MIT © utemix-lab
